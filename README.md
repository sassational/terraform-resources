# terraform-resources
This repository provides the minimum Terraform resource base required to build complex, high-level modules. It acts as a primitives library, containing the most granular definitions that, when combined, enable the operation of multiple Cloud services. Its the first step in developing more sophisticated Terraform modules.
